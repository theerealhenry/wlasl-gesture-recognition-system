# GRU — fewer parameters than LSTM, often comparable accuracy, faster inference
model:
  name: gru
  hidden_units: 128
  num_layers: 2
  dropout: 0.3
  bidirectional: false
  dense_units: 64
  activation: relu
